#ifndef __GCD_H__
#define __GCD_H__

#include <vector>
using namespace std;

template<class T>
T gcd(T a, T b);

template<class T>
T gcd_s(const vector<T>& vec);

template<class T>
T lcm(T a, T b);

template<class T>
T lcm_s(const vector<T>& vec);

#endif
